{
  "name": "portfolio-backend",
  "version": "2.0.0",
  "type": "module",
  "description": "Portfolio backend with GitHub webhook integration and PostgreSQL",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "build": "echo 'No build needed for serverless functions'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "pg": "^8.11.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}